name: UserService - Asp .Net Micro Service

on:
  push:
    branches: [ main, feature/initial-tooling ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      - name: Build and Publish UserService
        working-directory: microservices/UserService
        run: |
          dotnet restore
          dotnet build --configuration Release
          dotnet publish --configuration Release --property:PublishDir=./publish/UserService